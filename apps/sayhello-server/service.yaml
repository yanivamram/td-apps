apiVersion: v1
kind: Service
metadata:
  name: sayhello-server
  namespace: demo
spec:
  selector:
    app: sayhello-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
